#!/usr/bin/env python3


def run(config, dist_tree, train_labels, unif_tree):

    #test_labels = list(set(labels) - set(train_labels))
    print("testing")
    #print(labels)
    print(train_labels)
    print(dist_tree.func.map.keys())

    #for l in train_labels:
    #    t = neigh /// dist.tree
    #print(test_labels)
    pass

if __name__ == "__main__":
    pass
