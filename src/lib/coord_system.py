from .config import Config

class CoordSystem(list):
    @staticmethod
    def load(config: Config):
        return CoordSystem()

    @staticmethod
    def calculate(config: Config):
        return CoordSystem()
